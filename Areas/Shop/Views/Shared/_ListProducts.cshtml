@model IEnumerable<THUD_TN408.Models.ProductDetail>
@if(Model.Any()){
    @foreach (var product in Model)
    {
        <partial name="_ProductCard" model="product" />
    }
}else{
    <h4 class="text-center">Trống</h4>
}