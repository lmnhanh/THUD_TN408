<script>    
    function ModifyOrder(id) {
        event.preventDefault();
        var url = event.currentTarget.href.substr(location.origin.length, 99)
        $.ajax({
            type: "POST",
            url: url,
        }).done(response => {
            document.querySelector("#order_" + id).innerHTML = response
        })
    }
</script>
